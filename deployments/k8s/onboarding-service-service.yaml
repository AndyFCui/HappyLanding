apiVersion: v1
kind: Service
metadata:
  name: onboarding-service
  namespace: km-system
spec:
  selector:
    app: onboarding-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
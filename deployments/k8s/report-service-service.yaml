apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: km-system
spec:
  selector:
    app: report-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
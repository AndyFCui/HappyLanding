apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: km-system
  labels:
    app: auth-service
    version: v1
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
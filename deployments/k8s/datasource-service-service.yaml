apiVersion: v1
kind: Service
metadata:
  name: datasource-service
  namespace: km-system
spec:
  selector:
    app: datasource-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP